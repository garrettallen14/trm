# Core
torch>=2.1.0
numpy>=1.24.0
einops>=0.7.0

# Data
datasets>=2.14.0

# Training
wandb>=0.16.0
tqdm>=4.66.0
pyyaml>=6.0.1

# Evaluation
matplotlib>=3.8.0
seaborn>=0.13.0

# Optional: Flash Attention (install separately on GPU)
# pip install flash-attn --no-build-isolation
